
SELECT * 
FROM Users
WHERE CreateDate >= DATEADD(DAY, -30, GETDATE());

SELECT COUNT(*)
FROM Users
WHERE Email LIKE '%@example.com';

UPDATE Users
SET Email = 'newemail@example.com'
WHERE UserID = 123;
